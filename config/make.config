#Automatically generated by configure
#compilers and linkers

#config name
LABEL=abt

ifeq (${MIC}, true)
    ARCH=-xMIC-AVX512
    OMPC_FLAGS=-L/home/sugiyama/inst/omni-compiler_cilk/opt/lib \
               -L/home/sugiyama/inst/argobots/opt/lib \
               -L/home/sugiyama/inst/hwloc-1.11.4/lib \
               -lompc -labt -lhwloc -lpthread -qopenmp
else
    ARCH=-xHOST
    OMPC_FLAGS=-L/home/sugiyama/inst/omni-compiler_cilk/opt/lib \
               -L/home/sugiyama/inst/argobots/opt/lib \
               -L/home/sugiyama/inst/hwloc-1.11.2/lib \
               -lompc -labt -lhwloc -lpthread -qopenmp
endif

#compilers
ifeq (${DEBUG}, true)
    OMPC=icc -I${HOME}/inst/argobots/dbg/include
    OMPLINK=icc -labt -L${HOME}/inst/argobots/dbg/lib ${OMPC_FLAGS}
else
    OMPC=icc -I${HOME}/inst/argobots/opt/include
    OMPLINK=icc -labt -L${HOME}/inst/argobots/opt/lib ${OMPC_FLAGS}
endif
CC=icc
CLINK=icc

#compiler and linker flags

ifeq (${DEBUG}, true)
    OPT_FLAGS=${ARCH} -g3 -O0 ${OMPC_FLAGS}
else
    OPT_FLAGS=${ARCH} -O3 -ipo ${OMPC_FLAGS}
endif

CC_FLAGS=-std=gnu11
OMPC_FLAGS=-std=gnu11
OMPC_FINAL_FLAGS=

CLINK_FLAGS=
OMPLINK_FLAGS=
